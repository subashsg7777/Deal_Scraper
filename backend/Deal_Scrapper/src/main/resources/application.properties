spring.application.name=Deal_Scrapper

# Render injects PORT at runtime.
server.port=${PORT:8080}

# Prefer env vars in deployment. For local dev, fallback is localhost Mongo.
spring.data.mongodb.uri=${MONGODB_URI:${SPRING_DATA_MONGODB_URI:mongodb+srv://Backup_User:subashsg7777_db_user@cluster0.a0nrawg.mongodb.net//deal_scrapper?serverSelectionTimeoutMS=8000&connectTimeoutMS=5000&socketTimeoutMS=10000}}
spring.data.mongodb.database=${MONGODB_DATABASE:deal_scrapper}

# Comma-separated origins.
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,https://deal-scraper.vercel.app}
app.cors.allowed-origin-patterns=${APP_CORS_ALLOWED_ORIGIN_PATTERNS:https://*.vercel.app}